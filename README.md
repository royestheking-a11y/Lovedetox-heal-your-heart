
  # Finalize LoveDetox Webapp Design

  This is a code bundle for Finalize LoveDetox Webapp Design. The original project is available at https://www.figma.com/design/2xuwyZckaqfytOLjKIK1xv/Finalize-LoveDetox-Webapp-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  